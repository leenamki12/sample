export type PartType = {
    id: number | string;
    orderSequence: number;
    performanceCount: number;
    name: string;
    createdAt: Date;
    updatedAt: Date;
};

export type PartTypeFormKey = 'name';
